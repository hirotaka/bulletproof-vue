# Security: Disable install scripts to prevent supply chain attacks (Shai-Hulud)
ignore-scripts=true

# Allow specific packages that require build scripts
# Add packages here as needed when pnpm install fails
# Example: pnpm.onlyBuiltDependencies=["esbuild", "better-sqlite3"]
